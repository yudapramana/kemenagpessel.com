---
deployment:
      tasks:
        - export DEPLOYPATH=/home/kemena10/public_html  # Add /<app_folder> if required
        - /bin/cp -r * $DEPLOYPATH                        #copy all recursively to $DEPLOYPATH